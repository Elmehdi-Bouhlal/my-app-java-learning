package loginIterface;

import javax.swing.*;

public class Signin extends JFrame {
    public Signin(){
        setTitle("Sign in");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLayout(null);
        setSize(300,500);
        setLocationRelativeTo(null);
        setVisible(true);
    }
}
